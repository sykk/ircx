/** What the status bar reads. The plugin *screens* moved into the settings
 * window; this stayed, because the count in the status bar is the client's and
 * `grants.ts` is what turns a grant set into it. */
export { pluginStatus } from "./grants";
